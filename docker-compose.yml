services:
  nginx:
    image: nginx
    ports:
      - 82:80
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - portfolio

  portfolio:
    build:
      context: .
